<% if @list.empty? %>
	<p>No has adicionado tareas aún</p>
<% else %>
	<div>
		<h4>Tareas Adicionadas</h4>
		<ul class="list-group list-group-flush">
			<% @list.each do |item| %>
				<li class="list-group-item"><%= item["title"] %></li>
			<% end %>
		</ul>
	</div>	
<% end %>	